1. 两个 put() 线程可能同时插入落在同一个桶中的两个不同 key。它们都读取
   相同的旧桶头，再分别把各自的新 entry 写到 table[i]。后一次桶头写入会
   覆盖前一次写入，使先插入的 entry 无法再从桶头遍历到，因此造成丢键。

2. 每个桶各有一把 mutex。put() 从查找、更新或插入直到修改桶头的整个过程
   都持有对应桶锁；get() 也在遍历对应链表时持有同一把锁。这样同一桶内的
   链表和桶头串行访问，不会丢失更新；不同桶使用不同的锁，因而可以并行执行。
